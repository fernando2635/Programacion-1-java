package abstraccion;

public class clase {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
/*
 * // Abstracción de un Libro para una Biblioteca
 * public abstract class LibroBiblioteca {
 * 	String titulo;
 * 	String autor;
 * 	boolean disponible;
 * 	public abstract void prestar();
 * }
 * 
 * lo abstracto no tiene implementacion
 * si tiene al menos un metodo abstracto todo es abstracto
 * 
 * si es abstraco no puede crear objetos concretos, solo se pueden crear objetos concretos de clases que hereden de la clase abstracta y que implementen los metodos abstractos
 * puede contener metodos concretos (con implementacion) y atributos, pero no se pueden instanciar directamente
 * si una clase concreta hereda de una clase abstracta, debe implementar todos los metodos abstractos de la clase abstracta, o tambien puede ser una clase abstracta y no implementar los metodos abstractos, pero si es una clase concreta debe implementar todos los metodos abstractos
 * si una clase concreta hereda de una clase abstracta, puede tener sus propios atributos y metodos concretos, ademas de los metodos concretos y atributos heredados de la clase abstracta
 * si una clase concreta hereda de una clase abstracta, puede ser instanciada y utilizada como un objeto concreto, pero no se puede instanciar directamente la clase abstracta
 * si una clase concreta hereda de una clase abstracta, puede ser utilizada como un objeto concreto, pero no se puede utilizar como un objeto de la clase abstracta, ya que la clase abstracta no tiene implementacion concreta y no se pueden crear objetos concretos de ella
 * se puede tener clases hijas para explicar la clase padre, pero no se pueden crear objetos concretos de la clase padre, solo de las clases hijas que implementen los metodos abstractos de la clase padre
 * 
 * 
 * 
 */

